package org.rustyclipse.ui.editors;

import org.eclipse.swt.graphics.Color;
import org.eclipse.swt.widgets.Display;

public class RustyColorConstants {
	public final static Color KEYWORD = new Color(Display.getCurrent(), 128, 0, 0);
	public final static Color STRING = new Color(Display.getCurrent(), 0, 128, 0);
	public final static Color COMMENT = new Color(Display.getCurrent(), 0, 0, 128);
}
